# Write your MySQL query statement below
select distinct(contest_id) , round((count(contest_id)/(select count(*) from users))*100.0,2) as percentage
from Register    
group by contest_id  
order by percentage desc,contest_id asc;
